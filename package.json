{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "proxies requests to local boston server to bypass CORS issues",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "kg",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "request": "^2.88.0"
  }
}
